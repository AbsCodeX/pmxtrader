# apps/cli (reserved)

Terminal entry points live at the repo root:

- `./pmx` → `scripts/pmx.sh`
- `./scripts/kalshi-quickstart.sh`, `./scripts/polymarket-us-quickstart.sh`

No separate CLI app package is implemented here yet.
