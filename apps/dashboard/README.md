# apps/dashboard (deprecated placeholder)

The web command center is **`dashboard/` at the repo root**, served by `scripts/pmxt-dashboard-server.py`.

Do not add files here — use `/dashboard/index.html`, `/dashboard/css/`, `/dashboard/js/`.
