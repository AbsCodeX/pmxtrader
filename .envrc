# direnv: allow loading with `direnv allow`
# Then: pmxt-start (session here) · pmxt-terminal (new window, from anywhere via ~/.zshrc)
source_env scripts/pmxt-env.sh
source_env scripts/pmx-aliases.sh
